---
import Base from '../../layouts/Base.astro';
import PageHeader from '../PageHeader.astro';
import { t, getSitioL } from '../../i18n.mjs';

interface Props { lang?: 'es' | 'en' }
const { lang = 'es' } = Astro.props;

const sitio = getSitioL(lang);
const key = sitio.web3forms_key || '';
const email = sitio.contacto?.email || '';
const ciudad = sitio.contacto?.ciudad || '';
---
<Base titulo={t(lang, 'nav.contact')} descripcion={t(lang, 'meta.contact')} lang={lang}>
  <PageHeader eyebrow={t(lang, 'nav.contact')} title={t(lang, 'contact.title')} />

  <section class="section pg-section">
    <div class="container">
      <div class="contact">
        <div class="contact__form">
          {key ? (
            <form action="https://api.web3forms.com/submit" method="POST">
              <input type="hidden" name="access_key" value={key} />
              <input type="hidden" name="subject" value={t(lang, 'contact.subject')} />
              <input type="hidden" name="from_name" value={t(lang, 'contact.from_name')} />
              {/* honeypot anti-spam */}
              <input type="checkbox" name="botcheck" class="hp" tabindex="-1" autocomplete="off" />

              <label class="field">
                <span>{t(lang, 'contact.f_name')}</span>
                <input type="text" name="name" required autocomplete="name" />
              </label>
              <label class="field">
                <span>{t(lang, 'contact.f_email')}</span>
                <input type="email" name="email" required autocomplete="email" />
              </label>
              <label class="field">
                <span>{t(lang, 'contact.f_message')}</span>
                <textarea name="message" rows="6" required></textarea>
              </label>
              <button class="btn" type="submit">{t(lang, 'contact.submit')}</button>
            </form>
          ) : (
            <p class="aviso">
              <strong>{t(lang, 'contact.pending')}</strong>
              {email && <> <a href={`mailto:${email}`}>{email}</a></>}.
            </p>
          )}
        </div>

        <aside class="card-flat contact__info">
          <p class="kicker">{t(lang, 'contact.aside_kicker')}</p>
          <p class="contact__name">UDG-CA-1190</p>
          <p class="contact__sub text-muted">{sitio.adscripcion}</p>
          <hr class="rule" />
          {email && (
            <p class="contact__row">
              <span class="contact__lbl">{t(lang, 'contact.row_email')}</span>
              <a href={`mailto:${email}`}>{email}</a>
            </p>
          )}
          {ciudad && (
            <p class="contact__row">
              <span class="contact__lbl">{t(lang, 'contact.row_location')}</span>
              <span>{ciudad}</span>
            </p>
          )}
        </aside>
      </div>
    </div>
  </section>
</Base>

<style>
  .pg-section { padding-block-start: var(--sp-6); }

  .contact { display: grid; grid-template-columns: 1fr; gap: var(--sp-6); }
  @media (min-width: 820px) { .contact { grid-template-columns: 1.4fr 1fr; gap: var(--sp-8); } }

  .hp { position: absolute; left: -9999px; }

  .field { display: block; margin-bottom: var(--sp-3); }
  .field span {
    display: block;
    font-family: var(--font-mono);
    font-size: var(--fs-xs);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--udg-gray-700);
    margin-bottom: 6px;
  }
  .field input, .field textarea {
    width: 100%; font: inherit; color: var(--c-ink);
    background: #fff;
    border: 1px solid var(--udg-gray-300);
    border-radius: var(--radius-md);
    transition: border-color 150ms ease, box-shadow 150ms ease;
  }
  .field input { height: 40px; padding: 0 12px; }
  .field textarea { padding: 10px 12px; }
  .field input:focus, .field textarea:focus {
    outline: none;
    border-color: var(--udg-navy-700);
    box-shadow: 0 0 0 2px rgba(69, 90, 140, 0.25);
  }

  .aviso {
    padding: var(--sp-3) var(--sp-4);
    font-size: var(--fs-sm);
    background: var(--udg-info-bg);
    color: var(--udg-info-fg);
    border-left: 3px solid var(--udg-navy-700);
    border-radius: var(--radius-md);
  }

  .contact__info { align-self: start; }
  .contact__name { font-size: var(--fs-s2); font-weight: var(--fw-bold); color: var(--udg-navy-900); }
  .contact__sub { font-size: var(--fs-sm); margin-top: 2px; }
  .contact__info .rule { margin: var(--sp-3) 0; }
  .contact__row { display: flex; flex-direction: column; gap: 2px; margin-bottom: var(--sp-2); }
  .contact__lbl {
    font-family: var(--font-mono);
    font-size: var(--fs-xs);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: var(--udg-gray-700);
  }
</style>
